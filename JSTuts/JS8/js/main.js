// Conditionals: Ternary Operator

//Syntax
//condition  ? ifTrue : if False

/*let soup = "Chicken noodle soup"
let response = soup ? "Yes, we have soup" : "Sorry, no soup today"
console.log(response)*/

//using a more complex illustration
//syntax
//conditions ? ifTrue : iffalse

let soup = "Chicken Noodle soup";
let iscustomerBanned = true;
let soupAccess = iscustomerBanned ? "Sorry, no soup for you" : soup ? `There is ${soup} for you `
  